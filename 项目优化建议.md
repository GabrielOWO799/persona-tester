# 🧸 人格提示词测试助手 · 优化评审清单（通俗版）

> 阅读对象：想快速搞懂"这个项目现在哪里能跑、哪里会出事、该先改什么"的人。  
> 评审时间：2026-08-13 ｜ 评审范围：`E:\Vscode\persona-tester` 全部源码  
> 一句话结论：**MVP 主干功能是通的，但埋了几个"一点就崩"的雷，还有几个"说了有、其实没做"的功能。**

> ✅ **已修复（2026-08-13）**：① 报告保存崩溃（空文件→真目录 + 代码防护）；② 空 requirements.txt（已按实际版本补全）；③ 生成人格同名覆盖（改为可命名/自动命名）；④ 多轮对话失忆（persona_chat 接收 history，已验证历史进入 prompt）；⑤ 缺 .gitignore（已加，防密钥泄漏）；⑥ 顺手清理：删掉暴露给用户的调试输出、修掉 persona_chat 把函数对象当人设的潜伏 bug、评估器剥离 \`\`\`json 围栏防假 0 分（已验证）。  
> ✅ **阶段1 已修复（2026-08-13 晚）**：⑦ 刷新后恢复历史（新增 tools/persona_history.py，按人格落盘聊天记录 + 记录上次活跃人格，启动时自动恢复，已验证 round-trip）；⑧ API 错误兜底（新增 safe_tool 包裹所有 generate/chat/evaluate/optimize 调用，异常时 st.error 不白屏，对比模式同样包裹）；⑨ 严格模仿模式真正生效（strict=True 时把模板作为强约束注入提示词，已验证分支切换正确）。
> ✅ **阶段2 已修复（2026-08-16）**：⑩ 评分系统反谄媚改造（`persona_evaluator.py` 重写为 Tier-2：rubric 锚点 + 普通助手基线对比 + 3 评委自洽取中位数 + 硬规则检查 + 置信度；分数从虚高 8-9 降到有区分度的 4-6，已用真实 LLM 验证）；⑪ 测试套件（侧边栏编辑 data 里的 test_cases，一键跑出逐条 pass/fail + 合格率，见第 8 条）；⑫ 版本回归（同一套用例 vA vs vB 得分 delta + 合格率变化，表格展示）。
> ✅ **阶段3 已修复（2026-08-17）**：⑬ 缺口①裁判/生成解耦——侧边栏新增「⚖️ 裁判模型」选择器（默认 deepseek-chat，可切 deepseek-reasoner 或自定义名）；评估器的「标识抽取 + 3 次评分」改用裁判模型，基线"普通助手"仍用演员模型（保证 persona_lift 只反映人格差异，不反映模型差异）；评分展示带上「裁判 {model}」。缺口②人工相关性验证（需真人标注样本校准）仍待做。

---

## 先看懂这个项目在干嘛（30 秒版）

这是一个帮你**调教 AI 人格（角色扮演提示词）**&#x7684;小工具，界面是网页（Streamlit）：

- 📚 **人格库**：内置几个示范人格 + 你自己存的人格
- 🛠️ **生成人格**：你描述一句"一只爱撒娇的橘猫"，它帮你写出完整提示词
- 💬 **人格对话**：用人格去聊天，看它演得像不像
- 📊 **自动评分**：从"一致性 / 语言风格 / 角色身份"三个维度打分 + 给建议
- 🔧 **自动优化**：你说"不满意"，它改一版更好的人格
- 🔀 **版本管理**：同一个人格存多个版本，可切换
- 🆚 **对比模式**：左右并排比两个人格的回答

下面说的"坑"，都是在这个骨架上发现的。

---

## 健康度速览

| 维度     | 状态       | 说明                              |
| ------ | -------- | ------------------------------- |
| 核心对话链路 | 🟢 基本能跑  | 生成→对话→评分→优化 通了                  |
| 会崩的地方  | 🔴 有 3 处 | 保存报告、装依赖、生成人格覆盖                 |
| 宣称没做的  | 🟡 4 处   | 严格模仿 / 刷新恢复 / 多轮记忆 / test_cases |
| 工程化    | 🔴 偏弱    | 无依赖清单、无 .gitignore、无错误兜底        |
| 文档     | 🟡 基本没有  | 只有这份清单（README 缺失）               |

---

## 🚨 第一优先：会出事的 Bug（点一下就崩 / 偷偷丢数据）

### 1. 点"保存报告"必崩 ❗

- **现象**：你测完一问一答，点"保存本次测试"或"保存对比报告"，页面直接报错。
- **原因**：`data/test_reports` 现在是个**空文件**，不是文件夹。代码想往它里面写 `xxx.json`，系统说"这不是目录"，就炸了。
- **修复**：删掉这个空文件，建一个真正的 `data/test_reports/` 文件夹即可。
- **通俗比喻**：就像你想往"照片"这个文件里塞一张图，但"照片"本身是个 txt，当然塞不进去。

### 2. 装不上依赖（requirements.txt 是空的）❗

- **现象**：别人（或云端）想跑你的项目，`pip install -r requirements.txt` 什么都没装。
- **原因**：`requirements.txt` 文件存在，但里面一行字都没有。
- **修复**：写上 `streamlit / langchain / langchain-deepseek / langgraph / pydantic / python-dotenv / tenacity` 并锁版本。
- **影响**：没它，项目**没法复现、没法部署**，等于交不出去。

### 3. 生成的人格会"互相覆盖" ❗

- **现象**：你生成第一个人格（橘猫）→ 再生成第二个（冒险者），结果第一个不见了，两个挤在同一个人名下变成 v1/v2。
- **原因**：生成时名字被**写死成"新生成的人格"**（`app.py` 里固定字符串），第二次生成就并到同一个名字下。
- **修复**：生成时让用户填名字，或从描述里自动取个名。

### 4. "多轮对话"其实每次都失忆 ❗

- **现象**：你问"你叫什么？"它答"小美"；再问"我们刚才聊了啥？"它一脸懵，像从没见过你。
- **原因**：每次叫模型回答，只把**当前这句话**和它的人设发过去，**没把前面的聊天记录发过去**。界面上历史在，但模型那边没有。
- **修复**：把 `st.session_state.messages`（聊天记录）拼进发给模型的 prompt 里。
- **和你之前总结的关系**：你进度里写了"保持对话历史"，目前只在界面上成立，模型本身没记忆。

---

## ⚠️ 第二优先：说了有、其实没做的功能

### 5. "严格模仿参考模板"勾选框是摆设

- **现象**：侧边栏有个复选框"严格模仿参考模板风格"，勾不勾结果一样。
- **原因**：生成人格的函数里虽然写了个 `strict` 参数，但**函数体里根本没用它**；界面也没把勾选状态传过去。模板只是当"例子"塞进去，不是真"严格模仿"。
- **修复**：要么真正实现（把模板提示词更强地注入），要么先把勾选框去掉，免得误导。

### 6. "刷新后恢复历史"没做

- **现象**：网页刷新一下，聊天记录全没了。
- **原因**：记录只存在浏览器内存（session_state），**没存到硬盘文件**。
- **修复**：每次对话后把记录写到 `data/` 下的 json，启动时读回来（参考你另一个项目的做法）。

### 7. `agent.py` 整段是"死代码"

- **现象**：项目里有个 `agent.py`（带记忆的 Agent），但网页根本没用它。
- **原因**：真正跑的 `app.py` 是**直接调用各个小工具**，没走 `agent.py`。所以 `agent.py` 里的"多轮记忆"能力完全没被用上。
- **影响**：不算 bug，但容易让人误会"记忆功能很强"，其实没接上。建议删掉或真正接入。

### 8. `test_cases`（测试用例）写了没人看

- **现象（旧）**：参考库里给小美、乐迪精心写了"用户问 \_\_ 期望行为 \_\_"，但程序从不读取。
- **现状（2026-08-16 已修复）**：已接入「🧪 测试套件」功能，数据终于被用起来了：
  - 侧边栏按当前人格加载 `test_cases`（小美 2 条、乐迪 1 条），可图形化增删改、保存到 `user_personas.json` / `reference_personas.json`；
  - 点「🏃 运行测试套件」→ 逐条生成回答 → 用评估器打分 → 若设了 `expected_behavior` 则判定 `meets_expectation`（3 评委多数票）→ 主区域汇总**合格率**与每条证据缺失/建议；
  - 未设期望行为的用例标 ⚪ 不参与合格率统计，避免"假不及格"。
- **延伸（差异化卖点）**：同一套用例可对两个版本跑「🔁 版本回归」，直接看 vA→vB 的得分 delta 与合格率变化，是和同类竞品拉开差距的关键能力。

---

## 🧩 第三优先：代码里的隐患（不影响跑，但埋雷）

### 9. 一段会"认错人"的代码（潜伏 bug）

- `persona_chat.py` 里有行把**函数自己**当成人设文本拼进了提示词（变量名还拼错）。目前这行没被用到，但万一哪天启用就乱套。顺手清掉。

### 10. 给用户的"调试小字"

- `app.py` 里有一行 `st.write("当前使用的人格：…")`，本该是开发时看的，却直接显示给用的人。删掉。

### 11. 评分偶尔会"假 0 分"

- 模型如果给的评分结果外面套了 \`\`\`json 代码块，解析会失败，程序就默默返回"全 0 分"。用户以为人格真这么差，其实只是格式问题。修：解析前先把代码块符号剥掉。

### 12. 密钥安全问题 ❗（重要）

- 项目里 `.env` 存着**真实的 API Key**（调用大模型的"密码"），而且是明文。
- **好消息**：目前还没被 git 记录（没泄漏到仓库）。
- **风险**：项目**没有 `.gitignore`**，哪天你 `git init` 提交了，密钥就跟着进仓库、可能外泄。
- **建议**：① 立刻加 `.gitignore` 把 `.env` 排除；② 如果密钥曾外泄过，去平台**轮换（重置）**&#x4E00;个新 Key。

### 13. 没有"出错保护"

- 大模型偶尔会超时 / 限流。一旦出错，整个网页直接白屏报错，没有友好提示。建议包一层 `try/except` 显示"出错了，稍后重试"。

### 14. 小工具各自建连接（可优化）

- 每个功能模块都自己 new 了一个模型客户端，重复建。可以共用一个，省资源。

---

## 🎨 第四优先：用起来不够顺手

| 痛点          | 现在          | 建议                             |
| ----------- | ----------- | ------------------------------ |
| 回答是"整段蹦出来"  | 转圈圈后一次性显示   | 改成**逐字流式**显示，更跟手               |
| 对比模式不记历史    | 每问一次只显示当轮   | 和单人格模式一样**累积显示**               |
| 保存只存一问一答    | 报告里没有完整对话   | 存**整段对话 + 每次评分**               |
| 没有"清空对话a"按钮 | 只能刷新        | 加一个一键清空                        |
| 不能换模型/调温度   | 写死 DeepSeek | 加模型选择和温度滑块                     |
| 报告不能导出      | 只能存 json    | 加 **Markdown / PDF 导出**（你计划里有） |



---

## ✅ 做得好的，先别动

- 🟢 **评分用结构化解析**（Pydantic）：思路专业，结果稳定可机器读取。
- 🟢 **版本管理的数据结构**（`versions[]` 带时间和备注）：设计合理，只是被"同名覆盖"坑了。
- 🟢 **对比模式、参考库种子数据**都到位，基础盘不错。

---

## 🗺️ 你之前计划的"还没做"的功能（确认确实没有）

| 计划功能                    | 现状                                         |
| ----------------------- | ------------------------------------------ |
| 批量测试（上传测试集自动跑）          | ✅ 已做（测试套件 + 版本回归，复用 data 里的 test_cases）        |
| 报告导出（Markdown/PDF）      | ✅ 已做（tools/report_export.py，PDF 用 STSong-Light 中文字体） |
| 版本对比（并排看提示词差异）          | ✅ 已做（unified diff 视图）                         |
| 多模型接入（OpenAI/Claude/本地） | ❌ 未做                                       |
| 自定义评估维度（如"幽默感"）         | ✅ 已做（自定义维度已贯穿评估/对话/导出）                        |
| 云端部署                    | ❌ 未做（依赖清单 + .gitignore 已补，已具备基础部署条件）          |
| 多用户支持                   | ❌ 未做                                       |



---

## 🛠️ 建议先改的 5 个（性价比最高）

1. **报告保存崩** → 删空文件、建真目录（5 分钟，立刻止血）
2. **空 requirements.txt** → 补依赖并锁版本（部署/复现的前提）
3. **生成人格同名覆盖** → 让用户取名或自动取名（救数据）
4. **多轮对话失忆** → 把聊天记录拼进 prompt（核心体验）
5. **加 .gitignore + 密钥安全** → 防泄漏（安全底线）

> 这 5 个改完，项目从"能 demo"升级到"能稳定给别人用"。其余的（严格模仿、刷新恢复、导出、批量测试等）可以排第二期。

---

## 📎 附：小白术语卡

- **API Key**：调用大模型的"账号密码"，泄露=别人能用你的钱额度，必须保密。
- **Streamlit**：用 Python 写网页界面的库，本项目界面就是它。
- **session_state**：网页运行时存在浏览器内存里的临时数据，刷新就没。
- **few-shot（示例）**：给模型看几个"正确答案范例"帮它学样，本项目的"模板"就是这个思路。
- **Pydantic 解析**：把模型返回的乱七八糟文字，按固定格式（如评分表）规整成结构化数据。
- **requirements.txt**：列项目需要装哪些库的清单，`pip install -r` 一键装。

---

## 📋 修复记录：第 5 轮（2026-08-22，管线合并 + 杂项清理）

### 性能与体验（承接第 4 轮）
- **评估并发化**：标识抽取 ∥ 基线回答并行 → 3 评委并行，单条评估从 5 轮串行往返降到 2 轮；对比模式 A/B 两侧评估也并行（线程内不碰 `st.*`，错误回主线程渲染）。
- **结果缓存**：标识抽取按（人格提示词, 裁判模型）缓存、基线按（用户输入, 演员模型）缓存（均 temp=0 近似确定）；回归套件同一人格只真正抽取 1 次标识，固定题库第二次运行基线全命中。`clear_caches()` 供测试用。
- **一键回归按题并发**：默认 3 路（`--workers` 可调），进度回调改在主线程按完成数触发（Streamlit UI 对象非线程安全），结果保持题库原序。

### 管线合并（本轮重点）
- **统一执行引擎**：`test_suite.run_case / run_cases` 成为唯一执行链路（UI 测试套件、版本回归、一键回归、CLI 四处共用），删除了 app.py 里并行实现的 `run_test_suite` 及重复的 JSON 解析逻辑；单条失败记录 error 字段不再中断整批，全部失败才抛错（保留失败横幅语义）。
- **顺带修复两处隐患**：① 基线加载用 int 键、resume 过滤/合并用 str 键的混用会静默丢基线条目（统一为 str 键）；② resume 过滤后 `run_cases` 按列表位置覆盖 id 导致题号错位（改为尊重用例自带 id）。均有回归测试兜底。

### 杂项清理
- **路径集中**：新增 `config.py` 锚定项目根，data/、报告、聊天历史、基线全部走 config，任意工作目录运行不再写错位置；一键回归基线从根目录隐藏文件挪到 `data/regression/`。
- **依赖治理**：requirements.txt 删除零引用的 langgraph、pydantic（pydantic 由 langchain 传递引入），pytest 锁定 9.1.1。
- **死代码**：删除 `_last_user`、`import re`（唯一使用处已并入 config）等。
- **测试**：新增 `tests/test_run_cases.py`（7 个离线用例：顺序保持、双格式兼容、逐条错误记录、坏 JSON、基线写入/对比、resume 保留旧条目、全失败抛错），全套 15 个测试通过。

---

## 📋 修复记录：第 6 轮（2026-08-22，评委打分机制修复 1/2/3）

针对「评委打分结果不尽人意」的调查结论，落地三项无争议修复：

1. **移除强制差异化**（最大噪音源）：删除 RUBRIC/评判流程里的「相邻维度分差 ≥1」硬规定和 `_force_differentiate` 兜底（它固定扣最后一个维度，导致「角色身份」被系统性压低 1 分），改为软性提醒「确实相当可以同分」。各维度独立评分，重复评估的分数抖动应明显下降。
2. **硬规则标记抽取与匹配修复**：
   - 抽取提示词的示例改为「裸字符串」并明确给出错误/正确示例（原示例 `名字'阿尔德里克'` 会引导模型输出带注释前缀的标识，substring 永远匹配不上）；
   - 匹配前做规范化（NFKC 全角→半角、去引号类字符、忽略大小写与空白）；
   - 抽取失败不再静默：输出 `markers_extracted: false`，UI/Markdown/PDF 报告均显示「⚠️ 标识抽取失败」警告。
3. **多数决边界修复**：`meets_expectation` 从写死 `>=2` 票改为「有效评委过半数」（1 个评委时 1 票即通过）；新增 `valid_judges` 输出字段；评委不足 3 个时置信度直接降级为 low。

测试：test_evaluator_offline.py 扩展至 6 个用例（同分不再扣分、引号/全角匹配、单评委过半、抽取失败显式标记等），全套 19 个测试通过。

待定项（未做，需打分口味决策）：维度分治锚点（给三个维度各自的评分描述）、反奉承区间松紧调整。建议改完后对同一个人格跑两遍核心 15 题做 A/B 验证。
